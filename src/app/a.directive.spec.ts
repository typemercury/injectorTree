import { ADirective } from './a.directive';

describe('ADirective', () => {
  it('should create an instance', () => {
    const directive = new ADirective();
    expect(directive).toBeTruthy();
  });
});
