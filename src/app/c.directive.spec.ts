import { CDirective } from './c.directive';

describe('CDirective', () => {
  it('should create an instance', () => {
    const directive = new CDirective();
    expect(directive).toBeTruthy();
  });
});
